---

- hosts: all
  sudo: true
  vars:
#    fubarhouse_user: vagrant
    node_version: 9.0.0
    node_versions: []
    node_packages:
    - yosay
    yarn_global_packages:
    - name: gulp
      version: 3.9.0
      upgrade: yes
  roles:
    - fubarhouse.nodejs
    - role_under_test
